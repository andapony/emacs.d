# emacs.d

Personal Emacs configuration.  Deployed by the Ansible playbook in the
`setup` repo, which clones this to `~/projects/emacs.d` and symlinks
`~/.emacs.d` to it.

Extracted from the `dotfiles` repo; history from before the extraction
lives there.
